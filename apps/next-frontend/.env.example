# URL del backend. Se lee solo desde el servidor (route handlers y Server
# Components); no lleva prefijo NEXT_PUBLIC_ a proposito, para que ni la URL ni
# el token lleguen nunca al navegador.
BACKEND_URL=http://localhost:8080
