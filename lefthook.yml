commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}

# pre-commit:
#   parallel: true
#   commands:
#     eslint:
#       glob: "*.ts"
#       run: pnpm exec eslint --fix {staged_files}
#       stage_fixed: true
