commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}

# pre-commit:
#   parallel: true
#   commands:
#     oxlint:
#       glob: "*.ts"
#       run: pnpm exec oxlint --fix {staged_files}
#       stage_fixed: true
